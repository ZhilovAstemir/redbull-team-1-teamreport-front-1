import { render, screen } from "@testing-library/react";
import AsideBar from "../components/AsideBar/AsideBar";
import { Provider } from "react-redux";

describe("side bar component", () => {
  it("side bar render", () => {
    const brn = screen.getByRole("button");
  });
});
